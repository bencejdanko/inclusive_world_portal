# Importing Data

Users, Programs and Enrollments (User enrollments to programs) have been configured to be importable/exportable via csv, json and other structured formats.

1. Navigate to the `/admin` interface.
2. Select on `Users`, `Programs`, or `Enrollments`.
3. Select on `Import` on the upper right hand corner.