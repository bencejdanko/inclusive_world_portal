from django.apps import AppConfig


class FormsConfig(AppConfig):
    """Forms application configuration."""

    name = "inclusive_world_portal.forms"
    label = "portal_forms"
    verbose_name = "Forms"
    default_auto_field = "django.db.models.BigAutoField"
