apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: inclusive-world-portal
  labels:
    app: nginx
    component: proxy
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: nginx
